# 디폴트 매개변수는 함수의 선언에서 정의해주어야 하는 이유를 설명하라.  
- 1. 함수의 선언과 정의를 분리해 분할 컴파일 시 컴파일러가 함수의 선언을 담은 헤더파일만을 보고 코드를 검사하기 때문에 선언부에서 디폴트 매개변수가 정의되지 않으면 오류가 발생한다.
- 2. 디폴트 매개변수가 작동할 때 함수 내부에서 설정한 기본값으로 초기화 하는 것이 아니라 함수를 호출한 곳에서 컴파일러가 코드를 변경하는 것이기 때문에 컴파일러가 참고하는 선언부에 디폴트 매개변수가 정의되어 있어야 한다.
 
